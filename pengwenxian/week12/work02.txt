1. 什么是前后端分离

后端的主要职责是对数据进行处理，与数据库、中间件等交互，对数据处理后返回给前端。

前端的主要职责是以页面、小程序等方式与用户交互，将请求参数与后端交互获取数据，将数据通过页面渲染的方式与用户进行交互。

前后端分离是通过将页面与数据进行分离，通过接口调用的方式来进行数据传输，前后端可独立开发、部署，同时互不影响。

2. 历史对话如何存储。以及如何讲历史对话作为大模型的下一次输入

项目中存在session里面，通过session id 把历史对话提取出来在拼接到新对话prompt中作为新输入