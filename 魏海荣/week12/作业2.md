# 什么是前后端分离

后端只负责数据接口，处理对数据库、服务器等后台操作的逻辑实现

前端是渲染的页面：负责展示页面；处理用户交互逻辑，网页、小程序、app页面，这种用户能看到的都是前端

前后端分离：前端只负责向接口请求，后端只需要处理接口请求的数据，前端项目和后端项目完全拆分开，俩边独立开发，独立部署

# 历史对话如何存储。以及如何讲历史对话作为大模型的下一次输入

项目中存在session里面，通过session id 把历史对话提取出来在拼接到新对话prompt中作为新输入

