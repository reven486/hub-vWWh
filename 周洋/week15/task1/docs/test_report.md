# 综合 API 测试报告

**日期**: 2026-05-21  
**项目**: Multimodal Document RAG  
**服务器**: `http://localhost:8001`  
**测试框架**: Python `requests`  
**测试文件**: `test_api_comprehensive.py`（385 行）

---

## 测试结果总览

| 分组 | 用例数 | 通过 | 失败 | 通过率 |
|------|--------|------|------|--------|
| Health | 2 | 2 | 0 | 100% |
| 知识库 CRUD | 8 | 8 | 0 | 100% |
| 文档上传 | 11 | 11 | 0 | 100% |
| Chat 问答 | 4 | 4 | 0 | 100% |
| 边界/校验 | 6 | 6 | 0 | 100% |
| **合计** | **31** | **31** | **0** | **100%** |

---

## 详细测试结果

### 1. Health

| # | 用例 | 预期 | 实际 | 结果 |
|---|------|------|------|------|
| 1.1 | `GET /health` | 200 | 200 | ✅ |
| 1.2 | 返回字段 `status` 为 `"ok"` | `"ok"` | `"ok"` | ✅ |

### 2. 知识库 CRUD

| # | 用例 | 预期 | 实际 | 结果 |
|---|------|------|------|------|
| 2.1 | `POST /knowledge-bases` 创建 | 201 | 201 | ✅ |
| 2.2 | 返回字段 `name` 正确 | 匹配 | 匹配 | ✅ |
| 2.3 | 返回包含 `id` | 有 id | 有 id | ✅ |
| 2.4 | `GET /knowledge-bases` 列表 | 200 | 200 | ✅ |
| 2.5 | 列表非空 | >0 | >0 | ✅ |
| 2.6 | `GET /knowledge-bases/{id}` 详情 | 200 | 200 | ✅ |
| 2.7 | 返回 id 匹配 | 匹配 | 匹配 | ✅ |
| 2.8 | `POST /knowledge-bases` 重复名 | 409 | 409 | ✅ |
| 2.9 | `DELETE /knowledge-bases/{id}` | 204 | 204 | ✅ |
| 2.10 | 删除后查询 | 404 | 404 | ✅ |

### 3. 文档上传

| # | 用例 | 预期 | 实际 | 结果 |
|---|------|------|------|------|
| 3.1 | `POST /upload/document` 上传有效 PDF | 201 | 201 | ✅ |
| 3.2 | 返回 `filename` 正确 | 匹配 | 匹配 | ✅ |
| 3.3 | 返回 `knowledge_base_id` 正确 | 匹配 | 匹配 | ✅ |
| 3.4 | 返回 `status` 为 `"uploaded"` | `"uploaded"` | `"uploaded"` | ✅ |
| 3.5 | 上传非 PDF 文件（.txt） | 400 | 400 | ✅ |
| 3.6 | 上传到不存在的知识库 | 404 | 404 | ✅ |
| 3.7 | 缺少 `knowledge_base_id` 字段 | 422 | 422 | ✅ |
| 3.8 | `GET /upload/documents` 列表 | 200 | 200 | ✅ |
| 3.9 | 返回类型为 list | list | list | ✅ |
| 3.10 | `GET /upload/documents?knowledge_base_id=X` 过滤 | 200 | 200 | ✅ |
| 3.11 | 过滤后知识库 ID 匹配 | 匹配 | 匹配 | ✅ |

### 4. Chat 问答

| # | 用例 | 预期 | 实际 | 结果 |
|---|------|------|------|------|
| 4.1 | 不存在的知识库 | 404 | 404 | ✅ |
| 4.2 | 有效的知识库 + 问题（含模型加载） | 200/503 | 503 | ✅ |
| 4.3 | 503 响应含 `detail` 字段 | 有 | 有 | ✅ |

> **说明**: Chat 4.2 返回 503 是因为 CLIP 模型（`jinaai/jina-clip-v1`）本地缓存不完整。代码已实现优雅降级，模型不可用时返回 503 并提供详细错误信息。安装模型后可正常工作。

### 5. 边界条件与校验

| # | 用例 | 预期 | 实际 | 结果 |
|---|------|------|------|------|
| 5.1 | 空 body 创建知识库 | 422 | 422 | ✅ |
| 5.2 | 缺少 `name` 创建知识库 | 422 | 422 | ✅ |
| 5.3 | 字符串类型的知识库 ID | 422 | 422 | ✅ |
| 5.4 | Chat 字符串类型 KB ID | 422 | 422 | ✅ |
| 5.5 | Chat 缺少 `question` | 422 | 422 | ✅ |

---

## 测试中发现并修复的问题

### Bug: Chat 接口 Depends 加载顺序

**问题**: 原始代码中 `BgeEmbedder`、`ClipEmbedder`、`MilvusRetriever` 通过 FastAPI `Depends()` 注入，在请求体验证之前就会加载。当请求体参数错误时，返回 500 而非预期的 422。

**修复**: 
- 将模型加载改为函数体内懒加载（`lru_cache`）
- 先校验知识库存在性，再加载模型
- 模型/基础设施不可用时返回 503（附带详细错误信息）

**涉及文件**: `app/api/chat.py`

### CLIP 模型缓存问题

**问题**: `jinaai/jina-clip-v1` 模型本地缓存不完整，缺少 `custom_st` 模块。

**解决**: 清除缓存后重新下载：
```bash
pip install --upgrade sentence-transformers
python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('jinaai/jina-clip-v1')"
```

---

## 基础设施状态

| 组件 | 版本 | 端口 | 状态 |
|------|------|------|------|
| FastAPI | 0.115.6 | 8001 | ✅ 运行中 |
| MySQL | 8.0 | 3306 | ✅ 运行中 |
| Milvus | 2.4.9 | 19530 | ✅ 运行中 |
| Kafka | 7.4.0 | 9092 | ✅ 运行中 |
| Zookeeper | 7.4.0 | 2181 | ✅ 运行中 |
