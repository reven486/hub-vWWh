
### 1. 文本提取

**pdfplumber**
- 依赖 PDF 内嵌的文本层（矢量字体），对"数字原生"PDF 效果好。
- 可精确获取每个字符的坐标、字体、字号信息，适合需要位置感知的场景。
- 对扫描版 PDF（图片 PDF）无能为力，需要自行接入 OCR。
- 多栏、复杂排版时，提取顺序容易混乱（按 x/y 坐标顺序，不识别版面逻辑）。

**MinerU**
- 先做版面检测（Layout Analysis），再按阅读顺序重排文本块，单栏/双栏/复杂排版均能正确还原。
- 内置 OCR（基于 PaddleOCR，支持 109 种语言），扫描件自动触发 OCR 流程。
- 会自动删除页眉、页脚、页码等干扰内容。
- 输出内容语义连贯，适合直接喂给大模型或向量检索。

### 2. 表格处理

**pdfplumber**
- 基于规则（线段检测）识别表格边框，提取为 Python list 或 Pandas DataFrame。
- 对有明确边框线的规整表格效果稳定，速度快。
- 对无边框表格、合并单元格、跨页表格识别率低。

**MinerU**
- 使用专项表格识别模型，能识别有/无边框表格、复杂合并单元格。
- 输出为 HTML 格式，结构更完整，方便后续渲染或 LLM 理解。
- 对跨页表格有一定的拼接处理能力。

### 3. 公式处理

**pdfplumber**
- 不支持公式识别，公式会被当作乱码或普通文本片段输出。

**MinerU**
- 自动检测行内公式和独立公式，转换为 LaTeX 格式，适合学术论文场景。

### 4. 图像处理

**pdfplumber**
- 可以检测图像的位置和边界框，但不提取图像内容，不做图像描述。

**MinerU**
- 将页面中的图像裁切保存为独立文件，并在 Markdown 中以 `![](路径)` 引用。
- 结合多模态 RAG 时，图像可以直接被 CLIP/Qwen-VL 等模型检索和理解。
- 纯 CPU 模式可运行，但速度较慢；GPU 加速后解析速度大幅提升。
- 资源消耗明显高于 pdfplumber。
